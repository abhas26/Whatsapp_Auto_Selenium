This Python code automatically replies to a new message by a specified contact with the specified message.

It replies only after it recieves a new message.

It replies only with the specified message.
